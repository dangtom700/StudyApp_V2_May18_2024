name: study_app_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pyqt=5.15.7
  - tabulate=0.9.0
  - pip
  - pip:
    - PyQt5==5.15.7  # Fallback if conda pyqt doesn't work
    - pymupdf==1.23.8